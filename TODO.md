# Before this site goes live

Resolved:

- [x] **WhatsApp number** — `+27 79 310 3535` (WhatsApp Business enabled),
      wired into the nav, hero, package, contact section and floating button.
- [x] **Social links** — Instagram, Facebook and TikTok, all `@barsmusic`,
      added to the contact section and footer.
- [x] **Portfolio photos** — 5 real event photos added to the "Our Work"
      gallery (`assets/img/portfolio/`).

Still outstanding:

- [ ] **Email address** — currently `info@barsgraphics.co.za`, a guessed
      address based on a domain that may not exist yet. Confirm the real
      inbox and domain to use.
- [ ] **Package inclusions** — the R6,000 package description is written
      generally (no specific hours, photo counts, video length or turnaround
      time are promised) since those weren't provided. Add specifics if you
      want them stated on the site.
- [ ] **More portfolio photos** — the gallery has 5 photos so far; add more
      as they become available (the client has confirmed more are coming).
      Drop new files in `assets/img/portfolio/` and add a matching
      `<figure><img ...></figure>` in the `#work` section of `index.html`.
- [ ] **Domain** — the site currently has no custom domain configured. Once
      GitHub Pages is enabled, a domain (e.g. via domains.co.za, per Blue
      Monday Studios' standard) can be pointed at it.
